export type Student = {
    id: string
    name: string 
    email: string 
    birth_date: string
    class_id: string

}    
    //id, name, email, birth_date, class_id

export type Professor = {
    id: string
    name: string 
    email: string 
    birth_date: string
    class_id: string
}

